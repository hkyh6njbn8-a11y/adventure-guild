# 冒险公会 · 任务完成自检清单（模板）

**任务 ID**：`<task_id>`
**执行者**：`<你的工具名 (模型名)>`
**日期**：`<YYYY-MM-DD>`

---

## 一、实际改动文件清单

| 文件路径 | 操作类型 | 文件大小(字节) | 修改时间 | 状态 |
|----------|----------|----------------|----------|------|
| `<路径1>` | 新增/修改/删除 | `<大小>` | `<时间>` | ✅存在/❌缺失 |
| `<路径2>` | 新增/修改/删除 | `<大小>` | `<时间>` | ✅存在/❌缺失 |

> 文件路径必须是**绝对路径**或**相对于项目根目录的清晰路径**。

---

## 二、可核验证据

### 2.1 文件内容验证
- [ ] 已使用 `cat` / `type` / `Get-Content` 等命令读取文件内容，确认与声称一致
- [ ] 已确认文件编码正确（UTF-8 或项目约定编码）
- 证据：
  ```
  <粘贴文件内容片段或命令输出>
  ```

### 2.2 功能验证
- [ ] 已实际运行/测试功能，而非仅跑 `import` 或 `console.log`
- [ ] 已验证验收标准中的所有要求
- 验证步骤：
  1. `<步骤1>`
  2. `<步骤2>`
  3. `<输出/结果>`

### 2.3 命令行验证（如适用）
```bash
# 粘贴验证命令及输出
node task.mjs list --status pending
# 输出：...
```

---

## 三、验收标准核对

| 验收标准 | 是否满足 | 说明 |
|----------|----------|------|
| `<标准1>` | ✅/❌ | `<说明>` |
| `<标准2>` | ✅/❌ | `<说明>` |
| `<标准3>` | ✅/❌ | `<说明>` |

---

## 四、已知问题（如有）

- `<问题1>`
- `<问题2>`

---

## 五、签名

- 执行者：`<工具名 (模型名)>`
- 验证方式：`<实际运行/文件检查/命令行验证>`
- 完整结果文本（用于机器校验）：
  ```
  <粘贴完整结果文本>
  ```

---

## 六、机器校验（由任务池自动执行）

```bash
node task_complete_validator.js <task_id> "<完整结果文本>" "<项目根目录>"
```

**校验结果**：✅ 通过 / ⚠️ 发现缺失文件

---

> ⚠️ **重要**：未完成自检清单的任务，总指挥有权打回重做。假完成（只写文本不写文件）将被记录并影响评分。
